/** Responsive breakpoints used across the application (M25). */
// Use the drawer for tablet-sized viewports too. The six-item icon-only tab
// strip was ambiguous for sighted users and exposed only icon implementation
// names ("message", "thunderbolt", …) to assistive technology.
export const BREAKPOINT_MOBILE = 768;
export const BREAKPOINT_COMPACT = 768;
export const BREAKPOINT_DESKTOP = 1180;
