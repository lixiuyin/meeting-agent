# Shared only by the backend and frontend reverse proxy in Docker Compose.
# Keep provider credentials in backend/.env; do not add them here.
ENVIRONMENT=dev
API_KEY=
PRINCIPAL_PEPPER=
CORS_ORIGINS=http://localhost:8307
# Required when ENVIRONMENT is not a development/test value.
FRONTEND_AUTH_USER=
FRONTEND_AUTH_PASSWORD_HASH=
